<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/primary_bg">

<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <TextView
        android:id="@+id/successsinfo"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:gravity="center"
        android:layout_marginTop="100dp"
        android:textSize="16sp"
        android:textColor="@color/red"
        android:text="订单提交失败"/>
    <TextView
        android:id="@+id/fahuotip"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:gravity="center"
        android:textColor="@color/primary_font_color"
        android:layout_below="@+id/successsinfo"
        android:textSize="10sp"
        android:text="我们将尽快为您解决此次问题"/>
    <TextView
        android:id="@+id/btn_continue"
        android:layout_width="100dp"
        android:layout_height="30dp"
        android:text="再次尝试提交订单"
        android:layout_marginTop="30dp"
        android:background="@drawable/main_btn_radius"
        android:layout_below="@id/fahuotip"
        android:gravity="center"
        android:textColor="@color/white"
        android:textSize="12sp"
        android:layout_centerHorizontal="true"
        />
</RelativeLayout>
</RelativeLayout>